{% extends 'base.twig' %}

{% block body %}
    <h1 class="mb-3 mt-3">ТЫ на страницы картин!</h1>

    <div class="container">
    <div class="row">
         <div class="col-md-9">
             {% include 'section/posts.twig' with posts %}

             <div class="row">
                 <a href="{{ url('gallery') }}"> Blog</a>
             </div>
        </div>
        <div class="col-md-3">
            {% include 'section/about.twig' %}
        </div>
    </div>
</div>
{% endblock %}